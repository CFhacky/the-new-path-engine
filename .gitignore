# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Throwaway session state — never committed (README: no session state)
*.registry.json
*.state.json
*.session.json

# Editor/OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db

# Path Engine Codex build output — embeds book-verbatim text, so NOT committed
# (same law as the raw OCR: rebuild on demand with codex/build_codex.py)
codex/build/
